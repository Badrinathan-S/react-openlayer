import React from "react";
import { Button, Container } from "react-bootstrap";

const ShareLocation = () => {
  return (
    <Container>
      <div className="userButtons">
        <div className="Buttons">
          <Button className="Button">Share my location</Button>
        </div>
      </div>
    </Container>
  );
};

export default ShareLocation;
